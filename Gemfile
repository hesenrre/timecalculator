source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in timecalculator.gemspec
gemspec
group :development, :test do
  gem 'pry'
  gem 'pry-nav'
  gem 'pry-remote'
end
